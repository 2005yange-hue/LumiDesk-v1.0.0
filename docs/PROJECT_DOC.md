# 基于多模态大语言模型与长期记忆机制的智能桌面虚拟伙伴系统

## 项目开发文档

---

## 一、项目名称

基于多模态大语言模型与长期记忆机制的智能桌面虚拟伙伴系统设计与实现

---

## 二、项目背景

随着大语言模型（LLM）和多模态技术的发展，AI 助手已经能够进行自然语言对话和图像理解。然而，大多数 AI 助手缺乏持续存在感、长期记忆能力和稳定的人格设定。

本项目旨在开发一个运行于桌面的 AI 虚拟伙伴，不仅能够进行自然语言交流，还具备：
- 视觉环境感知（理解用户屏幕内容）
- 长期记忆（记住用户偏好和习惯）
- 人格系统（稳定的角色设定）
- Live2D 可视化呈现
- 主动交互能力

该系统不是普通聊天机器人，而是一个具有持续存在感的 AI 智能体。

---

## 三、项目目标

### 总体目标

开发一个运行于桌面的 AI 虚拟伙伴应用，实现：

1. 用户与虚拟角色自然语言交流（文本、流式输出）
2. AI 角色拥有固定人格和背景设定（角色：艾莉）
3. AI 能够长期记忆用户信息（向量检索 + 结构化存储）
4. AI 能够读取用户屏幕内容（多模态视觉分析）
5. AI 能够理解当前用户行为并主动提供反馈
6. Live2D 角色根据 AI 状态进行表情和动作变化
7. 支持多模型切换（OpenAI / DeepSeek / Qwen 等）

---

## 四、核心功能

### 4.1 基础聊天系统 ✅（已实现）

- 多轮连续对话
- 流式输出（SSE）
- 上下文理解（携带历史记录）
- 停止生成控制

### 4.2 角色人格系统 ✅（已实现）

- Character CRUD 接口
- 角色"艾莉"（温柔、理性、善解人意）
- PersonaBuilder 动态 Prompt 生成
- 多角色创建与切换
- JSON 文件持久化

### 4.3 模型配置系统 ✅（已实现）

- API Base URL、API Key、模型名称配置
- Temperature、MaxTokens 参数调节
- 预设模型快捷切换
- localStorage 持久化
- 运行时配置覆盖

### 4.4 长期记忆系统 📋（规划中）

- 关键信息自动提取
- 向量化存储与语义检索
- 记忆权重衰减机制

### 4.5 屏幕视觉感知 📋（规划中）

- 定时屏幕截图
- 多模态 VLM 分析
- 环境状态识别

### 4.6 情绪状态系统 📋（规划中）

- 开心度 / 信任度 / 亲密度 / 精力值
- 状态影响回复语气和角色表现

### 4.7 Live2D 角色系统 📋（规划中）

- 虚拟角色模型加载
- 表情切换、动作播放
- 口型同步

### 4.8 Agent 主动交互 📋（规划中）

- 定时状态评估
- 主动触发决策
- 系统通知推送

---

## 五、技术栈

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.5 | UI 框架 |
| TypeScript | ~5.7 | 类型安全 |
| Vite | ^6.0 | 构建工具 |
| Pinia | ^2.3 | 状态管理 |
| Element Plus | ^2.9 | UI 组件库 |
| Vue Router | ^4.5 | 前端路由 |
| SCSS | - | 样式预处理 |

### 桌面框架

| 技术 | 版本 | 用途 |
|------|------|------|
| Electron | ^33.3 | 桌面壳（窗口管理、截图、通知） |
| electron-builder | ^25.1 | 打包分发 |

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| NestJS | ^11.0 | 服务端框架 |
| Node.js | - | 运行时 |
| TypeORM | ^0.3 | ORM（规划中） |
| MySQL | - | 关系数据库（规划中） |
| Chroma / Milvus | - | 向量数据库（规划中） |
| Redis | - | 短期缓存（规划中） |

### AI 能力

| 技术 | 用途 |
|------|------|
| OpenAI API（兼容） | 大语言模型调用（支持 GPT / DeepSeek / Qwen） |
| 多模态 VLM | 屏幕内容理解（规划中） |
| Prompt 工程 | 角色人格、记忆提取、屏幕分析模板 |
| Embedding API | 长期记忆向量化（规划中） |

---

## 六、用户使用流程

```
启动应用
  │
  ▼
配置 API → 选择模型（预设或自定义）
  │
  ▼
进入聊天界面 ←→ 设置页（随时可切换）
  │
  ├─ 输入消息 → SSE 流式返回 → 显示回复
  │
  ├─ 停止生成（中途取消）
  │
  └─ 清空对话（重新开始）
```

### 后续扩展流程（规划）

```
屏幕感知定时触发 → VLM 分析 → Agent 决策 → 主动发言 / 保持沉默

长期记忆提取 → 向量存储 → 聊天时自动检索 → 注入上下文

情绪系统 → 根据交互更新 → 影响回复语气 + Live2D 表现
```

---

## 七、系统功能模块

| 模块 | 状态 | 位置 |
|------|------|------|
| Chat 聊天模块 | ✅ 已实现 | `server/src/modules/chat/` |
| LLM 模型调度 | ✅ 已实现 | `server/src/modules/llm/` |
| 模型配置（前端） | ✅ 已实现 | `src/views/SettingsView.vue` |
| Character 角色人格 | ✅ 已实现 | `server/src/modules/character/` |
| Memory 记忆系统 | 📋 规划中 | - |
| Vision 视觉感知 | 📋 规划中 | - |
| Emotion 情绪系统 | 📋 规划中 | - |
| Agent 主动交互 | 📋 规划中 | - |
| Live2D 角色展示 | 📋 规划中 | - |
| User 用户管理 | 📋 规划中 | - |

---

## 八、开发阶段规划

| 阶段 | 内容 | 状态 |
|------|------|------|
| **第一阶段：基础聊天** | Vue 聊天界面 + NestJS 后端 + LLM 调用 + SSE 流式 | ✅ 完成 |
| **模型配置** | 设置页 + localStorage 持久化 + 多模型切换 + 停止生成 | ✅ 完成 |
| **第二阶段：角色人格** | Persona Prompt 完善 + 回复风格控制 | 📋 规划中 |
| **第三阶段：记忆系统** | 信息提取 + 向量检索 + 长期记忆调用 | 📋 规划中 |
| **第四阶段：Live2D** | 模型加载 + 动作控制 + 表情切换 | 📋 规划中 |
| **第五阶段：视觉感知** | 屏幕截图 + VLM 分析 | 📋 规划中 |
| **第六阶段：Agent** | 主动行为 + 情绪系统 | 📋 规划中 |

---

## 九、项目创新点

1. **多模态大模型视觉感知** — AI 理解用户桌面环境
2. **长期记忆增强** — 跨会话记忆保持
3. **人格化 AI 角色** — 稳定的角色设定与表达风格
4. **Live2D 实时表现** — 情绪驱动的可视化反馈
5. **Agent 主动交互** — 从被动回答到主动关怀
6. **桌面环境深度融合** — Electron 原生能力的充分利用
